name: cybermatrix_tunnel
description: CyberMatrix Tunnel - Persian sing-box Android VPN client
publish_to: "none"
version: 1.0.0+10

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_sing_box: ^1.2.0
  mmkv: ^2.4.0
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
